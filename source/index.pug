include ./panel.pug

doctype html
html(lang='ru')
  body
    .sliders

      div(id="block")
        +panel({vertical: true, pinUp: true, interval: true, min: 200, max: 600, step: 0})
      div(id="block2")
        +panel({vertical: false, pinUp: false, interval: false, min: 0, max: 900, step: 5})

